#!/bin/sh

DROPBOX="~/Dropbox\ \(Personal\)"
PATH=$PATH:/usr/local/bin

/usr/local/bin/brew list | tr '\n' ' ' > "$DROPBOX/Data/Settings/brew.lst"
/usr/local/bin/brew cask list | tr '\n' ' ' > "$DROPBOX/Data/Settings/brew-cask.lst"

